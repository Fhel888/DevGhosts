<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppBlessFuture" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">@color/branco </item>
        <item name="android:textViewStyle">@style/TextViewPadrao</item>
        <item name="android:windowBackground">@color/Verde</item>
    </style>

    <style name="Theme.AppBlessFuture" parent="Base.Theme.AppBlessFuture" />
</resources>